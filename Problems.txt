Проблемы, с которыми я столкнулся во время выполнения задания:
1. Не отрисовывались GeoJSON-данные. Я пробовал добавлять Point и LineString, но на карте их не было видно. С этой проблемой я так и не смог разобратся.
2. При попытке отображения GeoJSON-данных, которые записаны в несолько строк, возникала ошибка в Javascript (Syntax Error) - такие строки не получалось преобразовать в Javascript-объект (я использовал для этого метод JSON.parse()). С этой проблемой тоже не разобрался. Сейчас для того, чтобы не появлялось ошибки, нужно добавлять GeoJSON-данные одной строкой.
3. Я устанавливал значения тегов "action" HTML-форм через Javascript-код. Так как URL одной и той же страницы мог быть разным, тег "action" было довольно сложно правильно установить. Потом я вспомнил, что можно использовать Url.Action, и это намного упростило работу.
